public enum Morality
{
    Good = 60,
    Neutral = 50,
    Evil = 40
}

public enum PersonType {
    Friend,
    Neutral,
    Enemy,
    Companion
}

public enum StateMachine {
    Idle,
    Move,
    Attack,
    Work,
    Follow,
    Hurt,
    Dead
}

public enum PlayerCharacter {
    MasterKnight,
    Hero
}