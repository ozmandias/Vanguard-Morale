public enum Morality
{
    Good = 60,
    Neutral = 50,
    Evil = 40
}

public enum PersonType {
    Player,
    Normal,
    Enemy,
    Companion
}

public enum PersonAction {
    Working,
    None
}

public enum PlayerCharacter {
    MasterKnight,
    Hero
}